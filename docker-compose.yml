version: '3'

services:
  api:
    build:
      dockerfile: dockerfile.yml
      context: .
      target: development
    volumes:
      - .:/usr/src/app
    env_file:
    - .env
    command: npm run start:dev
    ports:
      - 3000:3000
  